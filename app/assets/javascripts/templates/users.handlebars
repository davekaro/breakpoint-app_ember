<div class="page-header">
  <h1>Users</h1>
</div>

<p>{{#link-to "users.new" class="btn btn-primary"}}New user{{/link-to}}</p>

<div class="row">
  <div class="col-md-12">
    {{outlet}}
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-bordered table-striped">
      <thead>
        <tr>
          <th>Name</th>
          <th>Email</th>
          <th>Phone Number</th>
          <th>&nbsp;</th>
        </tr>
      </thead>
      <tbody>
        {{#each}}
          <tr>
            <td>{{fullName}}</td>
            <td>{{email}}</td>
            <td>{{phoneNumber}}</td>
            <td>{{#link-to"users.edit" this class="btn btn-default"}}Edit{{/link-to}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</div>

